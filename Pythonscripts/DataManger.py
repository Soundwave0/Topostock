import requests
import json
import pandas
import numpy


#get a stock API, receive data and get the nice bits
#include method that makes the data look nice
#turn data into nice numpy array
#create a normalized array
